#include <iostream>
using namespace std;

int main() {
  int length;
  cin >> length;
  int arr[length];
  for(int i = 0; i < length; i++) {
    cin >> arr[i];
  }
  int min = -1;
  int minPos = -1;
  for(int i = 0; i < length - 1; i++) {
    if(arr[i] < arr[i + 1]) {
      
    }
  }
}
